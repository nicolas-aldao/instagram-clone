import React from 'react';
import { BasicLayout } from '../../layouts/BasicLayout';

export default () => (
  <BasicLayout
    title="Not found"
    subtitle="This page doesn't exist">
  </BasicLayout>
);
